import {memo, FC, ChangeEvent} from 'react';import {Input} from "@/components/ui/input";import {Select, SelectContent, SelectGroup, SelectItem, SelectTrigger, SelectValue} from "@/components/ui/select";export const choiceArr = [    {value: 'dollar', label: '$'},    {value: 'string', label: 'String'},    { value: 'number', label:"Number" },    {value: 'boolean', label: 'Boolean'}] as {value:string, label:string}[]type ChoiceProps = {    handleInputChange: (event:ChangeEvent<HTMLInputElement>) => void    handleSelectChange: (value:string) => void}const Choice:FC<ChoiceProps> = ({handleInputChange, handleSelectChange}) => {    return (        <div className="grid py-4 grid-rows-2 gap-2">            <div className="grid grid-cols-2">                <Input placeholder="Text" name="choice-1" className="grid-cols-2" onChange={handleInputChange}/>                <div className="grid-cols-2">                    <Select  name="select-choice-1" onValueChange={handleSelectChange}>                        <SelectTrigger className="w-[180px]">                            <SelectValue placeholder="Select a choice"/>                        </SelectTrigger>                        <SelectContent>                            <SelectGroup>                                {choiceArr.map(({value, label}) => (<SelectItem key={value}  value={`${value}-1`}>{label}</SelectItem>))}                            </SelectGroup>                        </SelectContent>                    </Select>                </div>            </div>            <div className="grid grid-cols-2">                <Input name="choice-2" placeholder="Text" className="grid-cols-2" onChange={handleInputChange}/>                <div className="grid-cols-2">                    <Select  name='select-choice-2' onValueChange={handleSelectChange}>                        <SelectTrigger className="w-[180px]">                            <SelectValue placeholder="Select a choice" />                        </SelectTrigger>                        <SelectContent>                            <SelectGroup>                                {choiceArr.map(({value, label}) => (<SelectItem key={value} value={`${value}-2`}>{label}</SelectItem>))}                            </SelectGroup>                        </SelectContent>                    </Select>                </div>            </div>        </div>    )}export default memo(Choice);